Fix the following issues. The issues can be from different files or can overlap on same lines in one file.

- Verify each finding against the current code and only fix it if needed.

In @README.md at line 43, Update the README entry for EXCHANGE_RATE_API_KEY to specify which provider the key is for (e.g., "ExchangeRate-API (exchangerate-api.com)") or add a short note below the environment variables table clarifying the expected service and a signup link; ensure the variable name EXCHANGE_RATE_API_KEY and its description explicitly mention the provider (or allowed providers) so developers know which API to sign up for.

- Verify each finding against the current code and only fix it if needed.

In @README.md around lines 47 - 53, Add explicit instructions for running the listed Supabase migrations: update the section that enumerates the migration files (20260425_add_exchange_columns.sql, 20260425_rename_payment_reference.sql, 20260426_onboarding_notifications_settings.sql, 20260428_applications_count_trigger.sql, 20260428_rls_policies.sql) to show the exact CLI command to execute (e.g., the standard Supabase CLI command to reset/apply DB migrations) and mention the alternative of applying them individually in the listed order from supabase/migrations/. Ensure the command is consistent with the Getting Started section if that command exists.

- Verify each finding against the current code and only fix it if needed.

In @README.md at line 19, Update README.md Step 3 to include the exact Supabase commands to run migrations: instruct users to run "npx supabase db reset" to reset and apply all migrations in order (or, alternatively, run "npx supabase db push" or apply migrations individually), and note that these commands should be executed from the project root with the supabase/migrations/ folder present so migrations are applied in order.

- Verify each finding against the current code and only fix it if needed.

In @app/admin/AdminContent.tsx around lines 374 - 389, Both Approve and Reject buttons use the same boolean check (actionLoading === prof.id) so both show "Updating..." simultaneously; change the loading state to include the action type (e.g., actionLoading = { id, type } or separate actionLoadingId and actionLoadingType), update handleVerify to set the composite state before starting and clear it after success/failure, and change each LoadingButton's isLoading to check both prof.id and the correct action type (e.g., type === "verified" for Approve and type === "rejected" for Reject) so only the button whose action is in progress shows loading; update references to actionLoading and ensure cleanup on completion/error in handleVerify.

- Verify each finding against the current code and only fix it if needed.

In @app/admin/AdminContent.tsx around lines 84 - 96, handleViewDocument lacks error handling for fetch failures and non-OK responses; wrap the fetch/response.json flow in a try/catch, check response.ok before calling response.json, and handle errors by logging and showing a user-friendly alert when the request fails or when data.signedUrl is missing (keep the existing window.open(data.signedUrl, "_blank") on success). Specifically update the handleViewDocument function to: 1) use try/catch around the fetch and JSON parsing, 2) if (!response.ok) throw or handle an error with a clear message, and 3) on any catch or missing data.signedUrl, call console.error and alert("Could not load document.") (or a more descriptive message) to avoid unhandled exceptions.

- Verify each finding against the current code and only fix it if needed.

In @app/api/banks/route.ts around lines 29 - 37, Wrap the fetch in an AbortController with a setTimeout (e.g., 5–10s) and pass controller.signal into the existing fetch call that creates response; clear the timeout when the fetch completes; ensure the code catches an aborted fetch and handles it (returning a 504/timeout response or appropriate error) instead of hanging. Locate the fetch that assigns response in app/api/banks/route.ts and use the AbortController around that call, ensuring you still include the original headers (Authorization using process.env.PAYSTACK_SECRET_KEY) and cache options.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/initialize/route.ts around lines 87 - 98, The current code updates the contracts table (via supabase .from("contracts").update(...) setting ngn_amount_paid and exchange_rate_used for contractId) before calling Paystack initialization, which can leave inconsistent data if initialization fails; move the persistence of ngn_amount_paid and exchange_rate_used to after a successful Paystack initialization (i.e., after the Paystack initialize call returns success) or instead persist these values on the payment/transaction record rather than the contracts row (update the code paths that call supabase.update(...) for ngn_amount_paid and exchange_rate_used or create/update the payment record) so that the database is only mutated once the payment initialization succeeds.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/initialize/route.ts around lines 142 - 151, The console.log in route.ts is printing PII (email); remove the raw email variable from the log and either omit it entirely or log a non-identifying derivative (e.g., masked/email domain or hashed) instead; update the logging call that references contractId, agreedBudget, clientTotal, exchangeRate, ngnAmount, amountInKobo, reference, email to exclude email or replace it with a maskedEmail/emailDomain/emailHash value so logs no longer contain the full email address.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/transfer/route.ts around lines 149 - 160, The code currently parses recipientResponse.json() and inspects recipientData.status without checking the HTTP response; update the logic in the recipient creation flow to first check recipientResponse.ok (or recipientResponse.status) before calling .json(), and if the HTTP response is not OK, perform the same supabase rollback (.from("contracts").update({ payment_released_at: null }).eq("id", contractId")) and return a NextResponse.json error, optionally capturing/including the HTTP status and any error body; only parse JSON and evaluate recipientData.status when recipientResponse.ok is true.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/transfer/route.ts around lines 196 - 202, The post-transfer Supabase update using supabase.from("contracts").update(...) for fields professional_receives and platform_fee (filtered by contractId) lacks error handling; wrap the update call to inspect the returned error/response, log any error (including contractId, professionalReceivesUsd, platformFeeUsd and Paystack transfer id/status) to your logger, and if appropriate surface a retry or alert mechanism for reconciliation so that a successful Paystack transfer cannot leave the DB without recorded professional_receives/platform_fee.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/transfer/route.ts around lines 186 - 194, The code parses transferResponse into transferData and checks transferData.status without verifying the HTTP response; update the transfer handling to first check transferResponse.ok (or HTTP status) before calling transferResponse.json(), and if not ok, set the contract payment_released_at to null via supabase and return a NextResponse JSON error (including any response text/body for diagnostics); otherwise parse JSON and continue to check transferData.status as before. Ensure you reference transferResponse, transferData, supabase, contractId and NextResponse when applying the change.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/verify/route.ts around lines 15 - 22, The fetch to Paystack (the call that assigns paystackResponse) needs a timeout using AbortController: create an AbortController, pass controller.signal to fetch, start a setTimeout that calls controller.abort() after a configurable timeout (e.g., process.env.PAYSTACK_TIMEOUT_MS or default), and clearTimeout when the fetch completes; handle aborts by catching the AbortError and returning/throwing an appropriate timeout error for the caller. Ensure the Authorization header and reference usage remain unchanged and that you attach the signal to the existing fetch options so paystackResponse is still assigned when successful.

- Verify each finding against the current code and only fix it if needed.

In @app/api/paystack/verify/route.ts around lines 45 - 57, The current code skips ownership verification when user is null, allowing unauthenticated requests to proceed; fix by enforcing authentication first (if (!user) return NextResponse.redirect(new URL("/dashboard/client?payment=failed", request.url))) before querying supabase, then perform the existing ownerCheck (.from("contracts").select("client_id").eq("id", contractId).single()) and compare ownerCheck.client_id !== user.id to redirect on mismatch; ensure you reference the same symbols (user, contractId, supabase, ownerCheck, NextResponse.redirect, request.url) and do not run the contract activation logic unless both authentication and ownerCheck pass.

- Verify each finding against the current code and only fix it if needed.

In @app/api/webhooks/paystack/route.ts around lines 74 - 86, The Supabase update result from supabase.from("contracts").update(...) is not checked, so the code always logs success; modify the code that updates the contracts table (the supabase.update call in route.ts) to capture the returned { data, error } (or error/status) and only log "Contract {contractId} activated via webhook" when there is no error and the update affected rows (data present); if an error exists, log or handle the error (including details and the reference variable) and avoid treating the contract as activated.

- Verify each finding against the current code and only fix it if needed.

In @app/api/webhooks/paystack/route.ts around lines 101 - 112, Replace the ad-hoc fetch to `${process.env.NEXT_PUBLIC_APP_URL}/api/send-verification-email` used for admin alerts with a proper alerting call: either invoke a dedicated admin alert endpoint (e.g. `/api/admin/alerts`) or a shared alert service function instead of repurposing send-verification-email; validate that process.env.NEXT_PUBLIC_APP_URL is defined before making the request and do not swallow errors—catch and log or rethrow them using your logger; stop stuffing error text into the `professionType` field and instead send a structured payload (e.g. { type: "transfer_failed", reference, message }) and keep `professionalName`/`userId` for their intended semantics so consumers like sendVerificationEmail or the new alert handler receive correct fields.

- Verify each finding against the current code and only fix it if needed.

In @app/api/webhooks/paystack/route.ts around lines 25 - 28, Replace the direct string comparison (hash !== signature) with a timing-safe comparison using Node's crypto.timingSafeEqual: import crypto, convert both values to Buffer (e.g., Buffer.from(hash) and Buffer.from(signature)), check lengths and only call crypto.timingSafeEqual when lengths match, otherwise treat as invalid; update the condition around the hash/signature check in the webhook handler (where variables hash and signature are used and the NextResponse.json({ error: "Invalid signature" }, { status: 400 }) is returned) so it uses crypto.timingSafeEqual for verification.

- Verify each finding against the current code and only fix it if needed.

In @app/dashboard/professional/contracts/page.tsx around lines 51 - 81, The handleMarkComplete function currently ignores the Supabase update result and always updates local state and sends notifications; change it to check the response from createClient().from("contracts").update(...).eq("id", contractId") for errors (inspect the returned data/error or status) and only proceed to setContracts and notify the user when the update succeeded; on failure, log or surface the error, do not change local state, and ensure setCompleting(null) is still called (or revert any optimistic changes) so the UI stays in sync with the database.

- Verify each finding against the current code and only fix it if needed.

In @app/dashboard/professional/contracts/page.tsx around lines 214 - 223, The displayed budget uses Number(contract.agreed_budget) directly which yields NaN when contract.agreed_budget is null/undefined; update the rendering to coerce a safe numeric fallback (e.g., const budget = Number(contract.agreed_budget ?? 0) or use parseFloat with nullish coalescing) and then call budget.toLocaleString() for the top line and (budget * 0.95).toFixed(2) for the "You receive" line so both formatted values never show "NaN" and handle missing agreed_budget gracefully.

- Verify each finding against the current code and only fix it if needed.

In @app/jobs/[id]/apply/page.tsx around lines 58 - 77, The effect uses the raw id (from useParams) in Supabase queries which can be string | string[] | undefined; normalize and validate it first (similar to handleSubmit) by checking typeof id === "string" and converting otherwise or returning early on undefined, e.g., compute a jobId variable before calling supabase.from(...).select(...) and use that jobId in the queries and in the .eq("job_id", jobId) check so setJob, setError and the existing application lookup always receive a consistent string id.

- Verify each finding against the current code and only fix it if needed.

In @app/jobs/[id]/apply/page.tsx around lines 102 - 121, Wrap the fetch/response handling in a try/catch (or use try/catch/finally) around the existing POST to "/api/apply" so network failures won't cause an unhandled rejection and will always reset submitting; on catch setError to a user-friendly network error message (and optionally include error.message), ensure setAlreadyApplied is not set on network errors, and call setSubmitting(false) in the finally (or in the catch before returning) so the UI doesn't remain stuck; locate the current code that defines response/result and uses setError, setAlreadyApplied, and setSubmitting to implement these changes.

- Verify each finding against the current code and only fix it if needed.

In @app/jobs/page.tsx around lines 81 - 101, The current client-side filtering (filteredJobs) runs on the page-local jobs array fetched per page, causing filters to only affect the current page and making pagination/counts incorrect; fix by moving filtering to the server: modify the data-fetching path that populates jobs (the API call/component that fetches jobs in app/jobs/page.tsx) to send search, filterProfession, filterBudget, and filterRemote as query params and return filtered results plus a totalCount, then remove client-side filter logic (the filteredJobs calculation) and use the server-returned list and totalCount for rendering and the "X open jobs available" count; alternatively, if you choose client-side filtering, fetch all jobs before pagination and implement client-side pagination over the full dataset instead of filtering only the current-page jobs.

- Verify each finding against the current code and only fix it if needed.

In @app/jobs/page.tsx around lines 43 - 53, The Supabase query in fetchJobs lacks error handling; update fetchJobs to check for and handle Supabase errors (the response includes an error field) by either wrapping the call in try/catch or inspecting the returned error and logging it / setting an error state before returning early, and only call setJobs and setTotalCount when there is no error; reference fetchJobs, supabase, setJobs, and setTotalCount when locating the fix and ensure user feedback (e.g., set an error state or show a toast) or logging is performed on failure.

- Verify each finding against the current code and only fix it if needed.

In @app/jobs/post/page.tsx around lines 103 - 106, The code computes finalCountry from formData.location_country and customCountry but lacks validation when location_country === "Other"; ensure customCountry is present and non-empty before using finalCountry (e.g., in the submit/validation flow that builds finalCountry), and if it's empty add a validation error or return early; update the logic around finalCountry (the variables formData.location_country and customCountry and the submit/validation function that uses them) to check for customCountry.trim() when "Other" is selected and surface a user-facing error message or prevent submission.

- Verify each finding against the current code and only fix it if needed.

In @app/messages/[contractId]/page.tsx around lines 27 - 100, The init function lacks try/catch and an abort/cleanup to prevent state updates after unmount; wrap the async body of init in try/catch to handle and log errors (and call setLoading(false) in finally), and add an AbortController or a mounted flag checked before every state call (setUser, setUserRole, setContract, setMessages, setLoading) to avoid updating unmounted components; ensure router.push calls still run when appropriate and short-circuit further processing when aborted by checking the controller/flag after each await, and cancel any in-flight supabase listeners/requests on cleanup.

- Verify each finding against the current code and only fix it if needed.

In @app/payments/[contractId]/page.tsx at line 19, The code unsafely casts params.contractId to string; update the page handler in page.tsx to validate params.contractId before use: check that params.contractId is defined and is a string (or handle string[] by using the first element or joining elements as appropriate), and if invalid throw or return a controlled error/redirect (e.g. 400 or notFound) so contractId is never assumed to be a string; reference the contractId variable and params.contractId to implement this validation and error-handling path.

- Verify each finding against the current code and only fix it if needed.

In @app/professionals/[Id]/page.tsx around lines 102 - 106, The query in supabase.from("profiles").select("full_name, country, email, bio").maybeSingle() is returning an unused PII field; remove "email" from the select list so it becomes select("full_name, country, bio"). Update the call site in this component (the select on the "profiles" query used to load the professional page) to stop requesting email and verify no other code in the same scope expects an email property.

- Verify each finding against the current code and only fix it if needed.

In @app/professionals/[Id]/page.tsx around lines 189 - 219, The submit flow in handleSubmitReview doesn’t surface failures to the user; when supabase.insert returns an error the code only stops submitting but never sets any visible error state. Add a reviewError state (e.g., const [reviewError, setReviewError] = useState<string | null>(null)), setReviewError(error.message || "Failed to submit review") inside the error branch of handleSubmitReview, ensure setSubmitting(false) still runs, render the reviewError in the component UI similar to reviewSuccess, and clear it after a timeout (e.g., setTimeout(() => setReviewError(null), 3000)) so users get explicit feedback on failures.

- Verify each finding against the current code and only fix it if needed.

In @app/professionals/page.tsx around lines 156 - 159, The count shown uses filtered.length (the paginated page slice) instead of the total number of matching professionals; update the logic so the UI uses the total pre-pagination count (e.g., a variable like totalMatches or fullFiltered.length computed before slicing for pagination) when rendering the text in page.tsx (referencing the filtered variable and the pagination/slicing logic), and keep the pluralization logic the same but driven by that total value rather than filtered.length.

- Verify each finding against the current code and only fix it if needed.

In @app/professionals/page.tsx around lines 105 - 117, The current client-side "filtered" post-processing (variable filtered that filters professionals using search, getProfessionLabel, and filterProfession) conflicts with server pagination; move filtering into the Supabase query instead: remove the filtered array usage and add .ilike("profiles->>full_name", `%${search}%`) (or equivalent text search) and .eq("profession_type", filterProfession) clauses to the data fetch, and trigger the fetch (the useEffect/handler that populates professionals) whenever search, filterProfession, or page changes so the server returns already-filtered paginated results; alternatively, if you intentionally want client-side filtering, remove .range() from the Supabase query so you fetch all professionals and then paginate locally, but do not combine both approaches.

- Verify each finding against the current code and only fix it if needed.

In @app/profile/page.tsx around lines 28 - 70, The getData function inside useEffect should add proper try-catch-finally error handling around async calls (supabase.auth.getUser(), supabase.from("profiles").select(...).single(), and fetch("/api/banks")) so failures don't leave the component loading forever; wrap those calls in try blocks, handle auth failure by logging and redirecting via router.push("/login") or setting an error state, handle profile fetch errors by logging and clearing/setting sensible defaults via setProfile and setFormData, handle bank fetch errors by logging and setting setBanks([]), and ensure setLoading(false) is always called in a finally block so loading ends even on error.

- Verify each finding against the current code and only fix it if needed.

In @app/profile/page.tsx around lines 80 - 111, The handleSave function currently calls supabase.auth.getUser() without error handling; wrap the body of handleSave in a try-catch-finally so any thrown errors (from supabase.auth.getUser or the DB update) are caught, setMessage is updated with an appropriate error message inside catch, and setSaving(false) is always executed in finally; keep the existing success/failure logic for the .from("profiles").update(...) result inside try and reference handleSave, supabase.auth.getUser, setSaving, and setMessage when locating where to add the try/catch/finally.

- Verify each finding against the current code and only fix it if needed.

In @app/settings/account/page.tsx around lines 604 - 615, The two placeholder Link components in app/settings/account/page.tsx (the Links rendering "Terms and Conditions" and "Privacy Policy") currently use href="#" — replace these with the real routes (e.g., "/terms" and "/privacy") or conditionally hide/remove the Link elements until the pages exist; update the Link href props or wrap them with a feature flag/conditional check so no placeholder "#" is shipped.

- Verify each finding against the current code and only fix it if needed.

In @components/notifications/NotificationBellDropdown.tsx around lines 122 - 128, The formatDate function in NotificationBellDropdown.tsx currently hard-codes "en-GB"; change it to use the runtime/user locale instead (e.g., pass undefined to toLocaleString or use navigator.language or a provided locale prop/context) so dates are formatted per the user's locale; update the formatDate implementation to accept or derive locale dynamically (retain the existing options object) and ensure callers still pass a string value.

- Verify each finding against the current code and only fix it if needed.

In @components/notifications/NotificationBellDropdown.tsx around lines 93 - 96, The unread count is computed from the stale closure variable notifications; update the unread calculation to use the freshly updated notifications state created during the optimistic update instead of the outer notifications variable. In NotificationBellDropdown, when you perform the optimistic mark-as-read (the code that builds updatedNotifications / calls setNotifications), compute unread from that updatedNotifications (or inside the setNotifications functional updater) and call onUnreadCountChange?.(unread) with that value so the count reflects the new state; reference the variables updatedNotifications, setNotifications, notifications, and onUnreadCountChange to locate and replace the stale filter usage.

- Verify each finding against the current code and only fix it if needed.

In @lib/rateLimit.ts around lines 11 - 25, The checkRateLimit function can throw if Redis/Ratelimit.limit fails; wrap the await limiters[windowKey].limit(key) call in a try/catch (inside checkRateLimit) and implement a fail-open strategy: on any error catch it, log or instrument the error (using whatever logger is available) and return true so requests are not blocked; ensure you only catch around the Ratelimit.limit invocation (and not the limiter creation), referencing checkRateLimit, limiters[windowKey], and Ratelimit.limit.

- Verify each finding against the current code and only fix it if needed.

In @lib/rateLimit.ts around lines 4 - 7, Validate UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN before calling new Redis(...) and avoid non-null assertions; check process.env.UPSTASH_REDIS_REST_URL and process.env.UPSTASH_REDIS_REST_TOKEN at module init (or lazily inside a getRedis() factory), throw a clear Error with a descriptive message if either is missing, and only then instantiate and export the redis constant so missing env vars produce an explicit, actionable error instead of a cryptic runtime failure.

- Verify each finding against the current code and only fix it if needed.

In @.env.example around lines 19 - 20, The inline comment after the NEXT_PUBLIC_APP_URL value can be parsed as part of the value; remove the trailing inline comment and place the explanatory text on its own line above the variable (e.g., add a comment line beginning with # explaining local vs production URL), leaving NEXT_PUBLIC_APP_URL=https://surveyconnect.vercel.app on its own line so parsers read only the URL as the value.

- Verify each finding against the current code and only fix it if needed.

In @app/api/admin/signed-url/route.ts around lines 13 - 21, The DB query result from supabase.from("profiles").select("is_admin").eq("id", user.id).single() currently ignores errors; change the destructure to capture the returned error (e.g., { data: profile, error }) and handle it explicitly: if error exists, log or surface it and return a 500 response (NextResponse.json) with a generic message and include error.message for diagnostics; keep the existing 403 path for non-admins when profile is present but is_admin is false. Ensure you update the block around the supabase query and the subsequent if-checks that reference profile/is_admin.

- Verify each finding against the current code and only fix it if needed.

In @app/api/admin/signed-url/route.ts around lines 28 - 31, The storagePath derivation is vulnerable to path traversal and mishandles multiple "/verification-documents/" segments; update the logic that sets storagePath (the variable computed from path in app/api/admin/signed-url/route.ts) to: 1) extract the intended suffix using the last occurrence of "/verification-documents/" (e.g. use lastIndexOf rather than split()[1]) and 2) normalize and validate the result (use path.posix.normalize) and explicitly reject or strip any "../" or absolute path components so the final storagePath cannot escape the intended directory; if validation fails, return a 4xx error. Ensure you apply these checks wherever storagePath is later used to access Supabase storage or files.

- Verify each finding against the current code and only fix it if needed.

In @app/api/apply/route.ts around lines 85 - 87, The current handler returns error.message to the client (NextResponse.json({ error: error.message })), which can leak sensitive DB/schema info; change this to return a generic message like { error: "Internal server error" } with status 500, and log the full error server-side using your server logger or console.error (capture the error object referenced as error). Update the branch that handles errors in route.ts (the if (error) block) to call NextResponse.json with the generic message and call your server logging function (or console.error) with the original error for diagnostics.

- Verify each finding against the current code and only fix it if needed.

In @app/api/apply/route.ts around lines 65 - 83, Add and rely on a DB-level unique constraint for (job_id, professional_id) on the job_applications table, and update the insert flow in route.ts to catch the constraint violation from supabase.from("job_applications").insert(...) and translate it into a friendly 409 response (keep the pre-check using supabase.select(...) for nicer UX but do not assume it prevents duplicates). Specifically, ensure the database schema has the unique index and then detect the unique-constraint error returned from the insert call (inspect the error object returned by the insert) and return NextResponse.json({ error: "Already applied" }, { status: 409 }) when that constraint is violated; otherwise surface other errors appropriately.

- Verify each finding against the current code and only fix it if needed.

In @app/api/notify/route.ts around lines 13 - 20, The POST handler currently only authenticates the caller (in POST(request: NextRequest) using createClient and supabase.auth.getUser) but does not authorize sending to the requested recipientEmail; update the handler to enforce authorization by either (a) validating recipientEmail against a resource owned by the authenticated user—query the contract/job record(s) by recipientEmail and confirm the returned ownerId or userId matches user.id before sending—or (b) restricting the endpoint to internal/service calls only by requiring a shared service secret or Supabase service role key in a header and rejecting requests missing or failing that check; implement one approach consistently (add the DB lookup/ownership check referencing the same recipientEmail param and the user.id from supabase.auth.getUser, or add service-secret validation early in POST) and return 403 on failure.

- Verify each finding against the current code and only fix it if needed.

In @app/api/notify/route.ts around lines 44 - 55, Wrap the call to resend.emails.send(...) in a try/catch around the block that currently sends the email and returns NextResponse.json({ success: true });; in the catch, log the caught error (without leaking secrets) and return an error response via NextResponse.json({ success: false, error: error.message || 'Email send failed' }, { status: 502/500 }); also handle the Resend SDK error object if present (inspect the caught error for sdk-specific fields) so resend.emails.send and NextResponse.json are protected from uncaught exceptions.

- Verify each finding against the current code and only fix it if needed.

In @app/api/notify/route.ts around lines 37 - 42, The email templates in the bodies map interpolate user-controlled values (recipientName and fields on details like jobTitle, applicantName, amount, otherParty) directly into HTML which allows HTML injection; update the code that builds bodies (the bodies constant / NotifyEvent templates in route.ts) to sanitize/HTML-escape all interpolated values before embedding them (or switch to a templating/email library with auto-escaping such as React Email/MJML), ensuring recipientName, details.jobTitle, details.applicantName, details.otherParty, details.amount and any other user-originated fields are passed through the escape function or safe template engine when composing the strings and links.

- Verify each finding against the current code and only fix it if needed.

In @app/api/notify/route.ts around lines 22 - 28, The handler currently trusts the type assertion from request.json(), which can throw on bad JSON and allows invalid values for event, recipientEmail, recipientName and details (leading to undefined subjects[event]/bodies[event]); wrap request.json() in try/catch to return 400 on parse error, then perform runtime validation: check that event is one of the allowed NotifyEvent values or that subjects[event] and bodies[event] exist, validate recipientEmail format and presence, validate recipientName presence/length, and validate that details contains the required keys for the given event; return explicit 400 responses for validation failures and only proceed to lookup subjects[event]/bodies[event] when validated (or use a schema validator like zod/a joi to consolidate these checks).

- Verify each finding against the current code and only fix it if needed.

In @app/robots.ts at line 10, The sitemap entry in robots.ts uses process.env.NEXT_PUBLIC_APP_URL directly, which can be undefined; update the code that sets sitemap to validate and normalize NEXT_PUBLIC_APP_URL (or use a safe fallback like an empty string or configured default) before concatenating "/sitemap.xml". Specifically, ensure the logic around the sitemap property (where sitemap: `${process.env.NEXT_PUBLIC_APP_URL}/sitemap.xml` is set) checks for a truthy NEXT_PUBLIC_APP_URL, trims any trailing slash, and constructs a valid absolute URL or omits the sitemap if no valid base URL exists, returning a handled default to avoid "undefined/sitemap.xml".

- Verify each finding against the current code and only fix it if needed.

In @app/sitemap.ts around lines 4 - 5, The base URL constant (base) in app/sitemap.ts can include a trailing slash from NEXT_PUBLIC_APP_URL, producing malformed URLs; update the initialization of base (the base constant) to strip any trailing slashes from the environment value (falling back to the default "https://surveyconnect.vercel.app" if unset) so subsequent URL joins (e.g., "/jobs") do not produce double slashes—use a simple trim/remove-trailing-slash operation on the env value before assigning to base.

- Verify each finding against the current code and only fix it if needed.

In @lib/csrf.ts at line 4, The current check "if (!appUrl) return true" in lib/csrf.ts disables CSRF protection when NEXT_PUBLIC_APP_URL is unset; change it to fail closed by either throwing a clear configuration error or returning false instead of true so requests are denied when appUrl is missing; update the code path that reads appUrl (the appUrl variable and the function that validates request origin) to validate the env at startup and surface a descriptive error like "NEXT_PUBLIC_APP_URL not configured" or return false to block requests until configured.

- Verify each finding against the current code and only fix it if needed.

In @lib/csrf.ts around lines 2 - 5, The current origin check rejects requests when request.headers.get("origin") is null; update the logic in the CSRF check so that when origin is null you fall back to comparing the request's own URL origin (new URL(request.url).origin) against appUrl (and the allowed Vercel origin) — i.e., compute requestOrigin = origin ?? new URL(request.url).origin and then return requestOrigin === appUrl || requestOrigin === "https://surveyconnect.vercel.app"; also keep the existing early return when process.env.NEXT_PUBLIC_APP_URL is missing.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_applications_count_trigger.sql around lines 19 - 27, Add an UPDATE handler to keep counters consistent when job_applications.job_id changes: implement a new PL/pgSQL function update_applications_count() that checks IF OLD.job_id IS DISTINCT FROM NEW.job_id THEN decrements jobs.applications_count for OLD.job_id (use GREATEST(...,0) to avoid negatives) and increments jobs.applications_count for NEW.job_id, then RETURN NEW; and deploy it with a trigger named trg_upd_applications_count defined as AFTER UPDATE OF job_id ON job_applications FOR EACH ROW EXECUTE FUNCTION update_applications_count(); reference existing increment_applications_count/decrement_applications_count logic to keep behavior consistent.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_rls_policies.sql around lines 21 - 23, The current RLS policy applications_update_own on job_applications is too broad: it allows clients (via the EXISTS on jobs.client_id = auth.uid()) to UPDATE any column; split this into two narrower policies — keep a policy that lets professionals (professional_id = auth.uid()) UPDATE any of their own application rows, and add a separate policy for clients that only permits updates to status-related columns (or require status changes go through a SECURITY DEFINER function). Locate the applications_update_own policy and replace it with two policies (or modify client branch) restricting the client branch to a column whitelist (e.g., status fields) or routing client updates through a safe function, ensuring jobs and job_applications and auth.uid() checks remain used to verify ownership.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_rls_policies.sql at line 58, The policy "prof_profiles_write_own" on table professional_profiles currently uses FOR ALL with only a USING clause so INSERTs won't be checked; update the policy to include a WITH CHECK clause that mirrors the USING expression (e.g., WITH CHECK (id = auth.uid())) or replace the FOR ALL policy with explicit INSERT and UPDATE policies (CREATE POLICY "prof_profiles_insert_own" FOR INSERT WITH CHECK (id = auth.uid()) and CREATE POLICY "prof_profiles_update_own" FOR UPDATE USING (id = auth.uid()) WITH CHECK (id = auth.uid())) so new rows are properly authorized.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_rls_policies.sql around lines 43 - 45, The current RLS policy "notifications_own" on the notifications table uses FOR ALL which unintentionally allows INSERTs; update the policy so it only covers SELECT, UPDATE, and DELETE (or remove INSERT and create a separate, stricter INSERT policy) for the notifications table and ensure the policy condition remains USING (user_id = auth.uid()) so users can only read/modify/delete their own notifications but cannot insert new notifications themselves.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_rls_policies.sql around lines 50 - 53, The current INSERT policy reviews_insert_client only allows clients to insert reviews; update the policy logic for reviews so that INSERT permits auth.uid() to be the reviewer when the related contracts row exists and payment_released_at IS NOT NULL for either side—i.e., allow (contracts.client_id = auth.uid() AND reviewer_id = auth.uid()) OR (contracts.provider_id = auth.uid() AND reviewer_id = auth.uid()) when checking the contracts table in the EXISTS clause for reviews.contract_id; additionally, add a database uniqueness constraint on reviews (contract_id, reviewer_id) to ensure each party can only review once per contract.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_storage_policies.sql around lines 13 - 19, The policy "professionals_upload_own_folder" currently only enforces folder ownership (uses auth.uid() and storage.foldername(name)) but does not verify the user has a professional role; update the INSERT policy definition for bucket 'verification-documents' to include an additional role check (e.g., inspect JWT claims via auth.jwt() (app_metadata or custom claim path) or call a server RPC like has_role(auth.uid(), 'professional')) combined with the existing ownership check so the definition requires both role='professional' and auth.uid()::text = (storage.foldername(name))[1]; ensure you use the correct JWT claim path your app uses (app_metadata -> roles or custom claim) or the appropriate RPC name if your system usesone.

- Verify each finding against the current code and only fix it if needed.

In @supabase/migrations/20260428_storage_policies.sql around lines 14 - 29, The second INSERT policy 'authenticated_can_upload' allows any authenticated user to upload anywhere and thus overrides the folder check; replace it by merging its condition into the existing 'professionals_upload_own_folder' policy so the INSERT definition requires both auth.role() = 'authenticated' AND auth.uid()::text = (storage.foldername(name))[1]; update the VALUES for the policy with that combined definition (and remove the separate 'authenticated_can_upload' INSERT) while keeping the same bucket_id ('verification-documents'), operation ('INSERT') and ON CONFLICT DO NOTHING behavior.